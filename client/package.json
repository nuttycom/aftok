{
  "private": true,
  "devDependencies": {
    "esbuild": "0.20.2",
    "node-forge": "^1.3.1",
    "parcel": "^2.12.0",
    "purescript": "^0.15.15",
    "uuid-validate": "^0.0.3"
  },
  "scripts": {
    "build": "spago build",
    "copy-assets": "ln -sf ../dev/assets ./dist/",
    "serve": "npm run copy-assets && parcel dev/index.html --open",
    "build-parcel": "mkdir -p dist && cp dev/index.html dist/ && spago bundle --bundle-type app --outfile dist/index.js && parcel build dist/index.html --public-url /app/"
  },
  "dependencies": {
    "big-integer": "^1.6.48",
    "purty": "^6.3.0",
    "qrious": "^4.0.2",
    "spago": "^0.93.39",
    "spago-legacy": "^0.21.0",
    "uuid": "9.0.1",
    "uuid-validate": "0.0.3"
  }
}
